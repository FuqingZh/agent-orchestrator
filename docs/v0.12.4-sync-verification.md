# v0.12.4 synchronization verification

Status: local merge and bounded validation complete; remote PR validation and
merge remain pending

Date: 2026-08-14

## Exact source evidence

| Object | Exact value |
| --- | --- |
| Fork `main` before merge | `17748630b701367bc70edfab6155c272eb10595b` |
| Upstream stable tag | `v0.12.4` |
| Upstream stable commit | `c01ef2d39783c1bc9f99ed75abfb7e90e6c7d3bf` |
| Archive branch | `archive/fork-private-main-c55165a8` at `c55165a8b042715e6e613028d3d3039fff79d90f` |
| Candidate branch | `codex/sync-upstream-v0.12.4` |
| Candidate merge commit | `39d1c739e9869caded58c72379462917ea95e78b` |

The merge commit has exactly two parents: the fork `main` point and the
upstream stable point. The observed upstream `main` at
`c0b9cb16c206f8b08985c3f3ab08888b94b0c492` was not merged.

## Migration evidence

- Existing fork migrations `0037` through `0053` remain unchanged.
- Stable additions `0054` and `0083` through `0088` are present byte-for-byte
  with the upstream tag; their source paths, introducing commits, and hashes
  are recorded in [`upstream-migration-map.md`](upstream-migration-map.md).
- The embedded migration filenames contain no duplicate numeric prefixes.
- `migrate_upstream_sync_test.go` now asserts the v0.12.4 terminal version `88`
  while retaining the v0.11.2, v0.12.1, and fork collision lineages.

## Checks run

From the isolated full clone `/tmp/ao-sync-v0124-full` (Go commands below run
from its `backend/` directory):

```text
npm run sqlc                                      PASS
GOCACHE=/tmp/ao-sync-v0124-gocache \
GOMODCACHE=/tmp/ao-sync-v0124-gomodcache \
  go test ./internal/storage/sqlite/...            PASS
GOCACHE=/tmp/ao-sync-v0124-gocache \
GOMODCACHE=/tmp/ao-sync-v0124-gomodcache \
  go test ./internal/httpd/...                     PASS
GOCACHE=/tmp/ao-sync-v0124-gocache \
GOMODCACHE=/tmp/ao-sync-v0124-gomodcache \
  go test -p=2 ./...                               PARTIAL
GOCACHE=/tmp/ao-sync-v0124-gocache \
GOMODCACHE=/tmp/ao-sync-v0124-gomodcache \
  go build -p=2 ./...                              PASS
GOCACHE=/tmp/ao-sync-v0124-gocache \
GOMODCACHE=/tmp/ao-sync-v0124-gomodcache \
  go vet -p=2 ./...                                PASS
GOSUMDB=off GOPATH=/tmp/ao-sync-v0124-gopath \
GOMAXPROCS=2 GOLANGCI_LINT_CACHE=/tmp/ao-sync-v0124-lint-cache \
  go run github.com/golangci/golangci-lint/v2/cmd/golangci-lint@v2.12.2 \
  run --concurrency=2 --path-mode=abs               PASS (0 issues)
frontend: npm run typecheck, typecheck:e2e, npm test  PASS
frontend: npm run build:daemon                      PASS
packages/product-ui: typecheck, test, pack          PASS
packages/cloud-client: generate/drift, typecheck, test, pack PASS
git diff --cached --check                          PASS before merge commit
git diff --check HEAD^1..HEAD                     PASS
```

The first SQLite run exposed the stale v0.12.2 expectation of version `82`;
the test was updated to the v0.12.4 terminal version `88` and the complete
SQLite package suite passed on rerun. The first attempted partial clone was
preserved separately after its promisor fetch failed; the final candidate was
built in the full clone.

The bounded repository test run passed every package except the existing
tmux integration test
`TestRuntimeIntegrationSupervisedExitKeepsInteractiveShell`, which timed out
while looking for the supervised workload in the process tree. The same test
failed at the fork `main` parent under the same host and tmux `3.7b`, so this is
not a v0.12.4 synchronization regression. The canonical `npm run lint` is
therefore not green locally because its test step stops on that test; the
separate version-pinned golangci-lint run passed with an isolated cache.

Frontend renderer tests completed with 177 files and 2,226 passing tests (one
skipped). `browser-runtime:prepare` remains an external validation gap because
the pinned artifact download failed with `UND_ERR_SOCKET`; no source or lockfile
was changed to work around that transport failure. GitHub CI remains the
authoritative cross-platform and release-environment check.

## Pending gates and boundaries

- Push and create the fork synchronization PR, then read back its exact head.
- Run the repository's full CI and any required generated-artifact drift gate.
- Perform an independent current-head review; green CI is not a substitute.
- Do not merge until exact-head checks, review, and actionable-thread gates are
  satisfied.
- Do not deploy or change production AO in this task.
