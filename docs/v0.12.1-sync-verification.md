# v0.12.1 synchronization verification

Date: 2026-08-06

Branch: `codex/sync-upstream-v0.12.1`

This record bounds the evidence for the fork synchronization. It is source and
isolated-runtime evidence, not production deployment evidence.

## Exact inputs

- fork source base: `6198f14071ff3e897b546a099f2b700f97b050a8`;
- upstream `v0.11.2`: `c5523a6d0e51251b79555b95ddc7d2be59da0f50`;
- upstream `v0.12.1`: `1df40e93772c2c48e916870d9c3ddf8f29a69f84`;
- upstream common snapshot: `5f3e6bcd5a47bb7312f80cfc3966464a8f948cda`;
- upstream `main` observation: `4ae7de30f5cc10897c5cb740aa4941534505b33c`;
- upstream PR #3485 head: `aa32c3b28bc025e0e0d2a9daa6ba3a30933d487e`;
- upstream PR #3550 head: `bd7baa54e829c3426cdeefe345b8252d1c8ed746`.

The source merge does not include either follow-up PR. The plan and its
required re-pin were merged before the source branch was created.

## Merge and migration evidence

- The upstream tag is merged with a normal no-fast-forward merge; both
  histories remain reachable.
- Migration files `0045` through `0048` preserve the exact upstream source
  bytes and are recorded in [`upstream-migration-map.md`](upstream-migration-map.md).
- Append-only migration `0049_reconcile_v0121_native_lineage.sql` bridges the
  native v0.12.1 version rows without editing a shipped migration.
- The focused migration matrix passes for fresh, native v0.11.2, native
  v0.12.1, fork stop points 0039/0041/0044, the deployed version-0040
  collision, representative data, and a second migration pass.
- `npm run sqlc` and `npm run api` have been run with isolated caches; a clean
  second-generation check remains part of the final gate.

## Canonical gates

Pending at this checkpoint:

- backend lint, build, full tests, race tests, and vet;
- frontend typecheck, build, and Vitest suite; and
- the repository workflow validation command.

The exact command, environment, result, and any bounded infrastructure gap
will be recorded here before delivery.

## Isolated runtime canary

Pending at this checkpoint. The canary must use a disposable data directory,
run file, loopback port, tmux directory, and Git repository. It must not use
the installed AO service, live database, production credentials, or a user
tmux server.

## Evidence boundaries

This synchronization does not deploy a binary, mutate production AO state,
claim a real pull request, write to Linear, or evaluate upstream PR #3485 or
#3550 acceptance. GitHub Actions remains authoritative for platform-specific
and secret-dependent checks.
