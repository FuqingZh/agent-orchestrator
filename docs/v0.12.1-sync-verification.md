# v0.12.1 synchronization verification

Date: 2026-08-06

Branch: `codex/sync-upstream-v0.12.1`

Source merge commit: `6c2e3af0947f76492f11ea1ec5d4bff0af92b34f`

This record bounds the evidence for the fork synchronization. It is source and
isolated-runtime evidence, not production deployment evidence.

## Exact inputs

- fork source base: `6198f14071ff3e897b546a099f2b700f97b050a8`;
- upstream `v0.11.2`: `c5523a6d0e51251b79555b95ddc7d2be59da0f50`;
- upstream `v0.12.1`: `1df40e93772c2c48e916870d9c3ddf8f29a69f84`;
- upstream common snapshot: `5f3e6bcd5a47bb7312f80cfc3966464a8f948cda`;
- upstream `main` observation: `4ae7de30f5cc10897c5cb740aa4941534505b33c`;
- upstream PR #3485 head: `aa32c3b28bc025e0e0d2a9daa6ba3a30933d487e`;
- upstream PR #3550 head: `bd7baa54e829c3426cdeefe345b8252d1c8ed746`.

The source merge does not include either follow-up PR. The plan and its
required re-pin were merged before the source branch was created.

## Merge and migration evidence

- The upstream tag is merged with a normal no-fast-forward merge; both
  histories remain reachable.
- Migration files `0045` through `0048` preserve the exact upstream source
  bytes and are recorded in [`upstream-migration-map.md`](upstream-migration-map.md).
- Append-only migration `0049_reconcile_v0121_native_lineage.sql` bridges the
  native v0.12.1 version rows without editing a shipped migration.
- The focused migration matrix passes for fresh, native v0.11.2, native
  v0.12.1, fork stop points 0039/0041/0044, the deployed version-0040
  collision, representative data, and a second migration pass.
- `npm run sqlc` and `npm run api` have been run with isolated caches; the
  generated outputs are clean on a second generation.

## Canonical gates

The local canonical gates passed with bounded Go/test concurrency and isolated
cache roots:

- `npm run lint` (backend full tests plus `golangci-lint`): passed;
- `npm run frontend:typecheck`: passed;
- `cd backend && go build ./...`: passed;
- `cd backend && go test -race ./...`: passed;
- `cd backend && go vet ./...`: passed;
- `cd frontend && npm run package`: passed. The repository has no `build`
  script; `package` is its declared production Electron/Vite bundle path;
- `cd frontend && npm test`: 120 files and 1,592 tests passed;
- `cd frontend && npm run typecheck:e2e`: passed.

`npx @redwoodjs/agent-ci run --all` is the one bounded local gap. It stopped
before running workflows because `.github/workflows/frontend-release.yml`
requires the unavailable `AZURE_CLIENT_ID`, `AZURE_KEYVAULT_NAME`, and
`AZURE_TENANT_ID` variables. No values were invented; the exact-head GitHub
Actions result remains authoritative for that check.

## Isolated runtime canary

Passed on 2026-08-06 with candidate binary `6c2e3af` built from this worktree.
The evidence directory was `/tmp/ao-v0121-canary.J0Z4Jb`; it used port
`34121`, an isolated `AO_DATA_DIR`, run file, tmux directory, and a disposable
Git repository. The installed AO service, live database, production
credentials, and user tmux server were not used.

The canary proved:

- `/healthz` and `/readyz` returned `ok` and `ready`;
- project `canary` was registered and survived daemon restart;
- worker session `canary-1` spawned through the shipped `codex` adapter, with
  a local no-model shim receiving the initial prompt and `ao send` follow-up;
- kill, restore, and a second kill completed, and the session remained
  durable after stop/restart with the same ID and `isTerminated: true`;
- the Dashboard-facing session DTO, project list, and notifications API were
  readable after database reopen (`notifications: []` was valid); and
- the isolated tmux server had no remaining sessions and the disposable Git
  repository had no leftover AO worktree after final stop.

The repository's `fake` harness was not used because it is intentionally test
only and is absent from the production adapter registry; the local codex shim
kept the canary model-free without changing the candidate source.

## Evidence boundaries

This synchronization does not deploy a binary, mutate production AO state,
merge a pull request, write to Linear, or evaluate upstream PR #3485 or #3550
acceptance. GitHub Actions remains authoritative for platform-specific and
secret-dependent checks.
