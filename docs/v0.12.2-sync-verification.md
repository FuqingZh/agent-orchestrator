# v0.12.2 synchronization verification

Date: 2026-08-10

Branch: `codex/sync-upstream-v0.12.2`

Draft PR: [#26](https://github.com/FuqingZh/agent-orchestrator/pull/26)

Candidate head at PR creation: `ad2b9e67934042f2f850410db5676cb6d23d57fe`

This record bounds the evidence for the fork synchronization. It is source
and local-validation evidence, not production deployment evidence. The PR
head and GitHub checks must be read back again after every later head change.

## Exact inputs

- fork source base: `c55165a8b042715e6e613028d3d3039fff79d90f`;
- immutable archive ref: `archive/fork-private-main-c55165a8` at
  `c55165a8b042715e6e613028d3d3039fff79d90f`;
- common upstream `v0.12.1` baseline: `1df40e93772c2c48e916870d9c3ddf8f29a69f84`;
- upstream stable `v0.12.2`: `b6609ae610e809309be86fce56c0845cc45628cb`;
- upstream `main` observation: `3e2dbf4249793e127f4e26b2499738ba3ca8fa1a`.

The release was observed as a published, non-draft, non-prerelease GitHub
Release on 2026-08-09. The observed upstream `main` remains outside this
stable synchronization.

## Merge and migration evidence

- The source merge is `6b008f9fbbbd30619d48675137af6b93f7e39b49`, a normal
  no-fast-forward merge with the exact release commit as its upstream parent.
- Migration reconciliation and generated-consumer adaptation are in
  `59af3110d57f2a603bf58b8895bca38dbbc4e4d2`.
- Fork-only runtime behavior was not selectively restored. Published fork
  migration history is preserved, and upstream migration bytes are retained
  under mapped `0050`/`0051` paths without editing their content.
- The migration matrix and second-pass schema/data checks passed for fresh,
  native upstream v0.11.2/v0.12.1, fork stop points 0039/0041/0044, the
  deployed version-0040 collision, representative rows, and repeat migration.
- `npm run sqlc` and the API generation path were each run twice; the second
  pass was clean.

## Local validation

Passed:

- bounded `go build -p=2 ./...` and `go vet -p=2 ./...`;
- bounded golangci-lint v2.12.2 (`0 issues`);
- frontend typecheck;
- all 159 renderer test files (2,097 passed, 1 skipped); and
- landing production build, including 92 static pages and 55 Markdown twins.

Both bounded full Go test and bounded full Go race test exercised the complete
package set. Every package passed except the intentional Codex provider
conformance test: the committed protocol is generated from `codex-cli 0.146.0`,
while this host provides `codex-cli 0.144.1`, which lacks
`app/read`, `app/installed`, `thread/environment/connected`,
`thread/environment/disconnected`, and
`externalAgentConfig/import/recordHistory`. The release protocol and check
were not weakened or regenerated against the older provider.

The unbounded `npm run lint` wrapper encountered host `fork/exec` resource
exhaustion before completing; the bounded equivalent reached the same provider
conformance result and the bounded linter passed. Local workflow validation
also stopped before executing all workflows because
`frontend-release.yml` requires unavailable `AZURE_CLIENT_ID`,
`AZURE_KEYVAULT_NAME`, and `AZURE_TENANT_ID`; no dummy values were supplied.

## Review and canary boundary

At this record's creation, exact-head GitHub checks were still running and no
independent current-head review verdict was claimed. React Doctor's concrete
diagnostics, actionable-thread count, and required CI remain authoritative on
PR #26. The isolated runtime release canary has not been run in this task.

No production AO binary, database, service, Dashboard, or Electron state was
changed. Production candidate creation and cutover remain separately
unauthorized.
