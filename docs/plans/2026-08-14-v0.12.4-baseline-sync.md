# AO fork v0.12.4 baseline synchronization

Status: local merge and bounded validation complete; pull request and any later
merge are pending

Date: 2026-08-14

## Goal

Make the fork's source tree follow the immutable upstream `v0.12.4` release
without selectively carrying fork-only runtime behavior. Preserve the fork's
published history and migration compatibility, then deliver the result through
a compatibility-sensitive pull request.

This task is source synchronization only. It does not build, install, restart,
replace, or migrate the production AO binary, database, service, Dashboard, or
Electron state. It does not modify PR #3550 or PR #28.

## Pinned inputs

| Input | Exact ref | Role |
| --- | --- | --- |
| Fork `main` | `17748630b701367bc70edfab6155c272eb10595b` | merge base |
| Upstream stable `v0.12.4` | `c01ef2d39783c1bc9f99ed75abfb7e90e6c7d3bf` | synchronization target |
| Fork archive | `archive/fork-private-main-c55165a8` at `c55165a8b042715e6e613028d3d3039fff79d90f` | recovery point |
| Upstream `main` observation | `c0b9cb16c206f8b08985c3f3ab08888b94b0c492` | explicitly excluded unreleased drift |

## Boundaries

- Use one isolated writer and a normal `--no-ff` merge; do not rebase or
  force-push published fork history.
- Treat upstream `v0.12.4` as authoritative for runtime, API, frontend,
  agent, reviewer, and adapter behavior.
- Preserve already-published fork migrations byte-for-byte. Add the stable
  release's new migrations unchanged when their numeric versions are unused.
- Keep worker-process containment and its separate upstream contribution out
  of this baseline.
- Do not cherry-pick commits from upstream `main` after `v0.12.4`.

## Acceptance

1. The candidate contains the exact stable commit as a parent and preserves
   the fork `main` parent.
2. The migration directory has one file per numeric version; all new stable
   migration files match their upstream SHA-256 values.
3. `npm run sqlc` is clean on a second pass and the generated API boundary has
   no drift.
4. SQLite lineage, HTTP/API, backend, frontend, and repository checks pass or
   have an explicit recorded gap.
5. The final branch head, PR head, checks, review, and unresolved threads are
   read back before any merge request; production remains unchanged.

## Execution record

- Sync branch: `codex/sync-upstream-v0.12.4`.
- Merge commit: `39d1c739e9869caded58c72379462917ea95e78b`.
- Merge parents: fork `17748630b701367bc70edfab6155c272eb10595b`, upstream
  `c01ef2d39783c1bc9f99ed75abfb7e90e6c7d3bf`.
- `go build -p=2 ./...`, `go vet -p=2 ./...`, focused SQLite/HTTP tests, the
  generated-artifact checks, frontend tests/typechecks, package checks, and
  the version-pinned golangci-lint run passed.
- The bounded full Go test run has one host-reproducible pre-existing tmux
  integration failure; the exact test also fails on the fork parent and is not
  being changed as part of this source synchronization.
- The browser-runtime artifact preparation is externally blocked by a socket
  failure; GitHub CI remains the authoritative environment check.
- Fork-only runtime behavior remains represented by the archive reference and
  is not selectively restored.
- The current upstream `main` snapshot and PR #3550 containment line remain
  separate work.
